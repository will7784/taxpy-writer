# Telegram
python-telegram-bot>=21.0

# LLM (escritura con GPT-4o + embeddings, o Gemini si GEMINI_API_KEY esta seteada)
openai>=1.55.0
google-genai>=2.0.0

# Voz (OpenAI Whisper + TTS)
pydub>=0.25.0

# Exportador DOCX
python-docx>=1.1.0

# Web server + templates
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
python-multipart>=0.0.17
itsdangerous>=2.2.0

# MCP (Model Context Protocol) — expone las tools del backend a agentes/harness externos
# v1.x: FastMCP API estable documentada por el harness de DeepSeek (@deepseek-ai/dsh-mcp-client).
# Importante: fastapi==0.115.0 exige starlette<0.39 (>=0.37.2). mcp acepta starlette>=0.27,
# pero sse-starlette 3.x exige starlette>=0.49.1 y rompería fastapi, así que fijamos
# sse-starlette<2 (1.x funciona con starlette 0.38). No cambiar esto sin revisar.
mcp>=1.28,<2
sse-starlette>=1.6.1,<2
starlette>=0.37.2,<0.39

# PDF processing (pdfplumber: texto nativo; PyMuPDF: rasteriza escaneos para OCR vision)
PyPDF2>=3.0.0
pdfplumber>=0.10.0
PyMuPDF>=1.24.0

# Utilidades
python-dotenv>=1.0.0
rich>=13.0.0
httpx>=0.27.0
truststore>=0.10.0
PyYAML>=6.0

# Validación de schemas (parser legal + salidas estructuradas de LLM)
pydantic>=2.9.0

# Force rebuild 20260526
